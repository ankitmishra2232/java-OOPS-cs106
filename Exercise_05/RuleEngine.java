
public class RuleEngine {
	public static void generateresult(StudentBatch batch) {
		// loop batch
		Course[] courses = new Course[3];
		for (int i=0;i<batch.students.length;i++) {
			
			

		}
		// calculate %  and if 40%+ then isPass= true
	}
}
